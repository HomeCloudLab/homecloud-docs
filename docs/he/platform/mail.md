# דואר (Mail)

HomeCloud Mail הוא שירות דואר ב-control plane מעל מנוע **Stalwart**.

## ארכיטקטורה

| שכבה | תפקיד |
|------|--------|
| Console `/console/mail` | רשימת תיבות + כרטיס מטא-דאטה של השירות |
| Console `/console/mail/{mailboxId}` | לקוח אימייל מקצועי בפריסת 3 פאנלים |
| `homecloud-api` `/accounts/{id}/mail/*` | JWT + metadata ב-Postgres |
| Stalwart (K3s) | SMTP/IMAP + תוכן ההודעה (מקור האמת) |

גופים וקבצים מצורפים **לא** נשמרים ב-Postgres.

## ניווט בקונסול

אותו דפוס כמו SO / Queues: **רשימה → פרטי משאב**.

1. **`/console/mail`** — טבלת תיבות (תוכן ראשי), כפתור **צור תיבה**, ופאנל מתקפל **סטטוס שירות ו-DNS**.
2. **`/console/mail/{mailboxId}`** — לקוח אימייל 3-פאנלים:
   - **סרגל צד** — נכנס, יוצא, אשפה, כתיבה עם מונה הודעות שלא נקראו
   - **רשימת הודעות** — אווטרים, נושא, תצוגה מקדימה, תאריכים יחסיים, נקודת "לא נקרא"
   - **תצוגת הודעה** — רינדור HTML מאובטח (DOMPurify), fallback לטקסט, סרגל פעולות

אין טאבים גלובליים של Messages / Domains / Settings בעמוד הרשימה.
חלק הסטטוס מקופל כברירת מחדל כדי שהתיבות יישארו במרכז.

## תכונות לקוח אימייל

### כתיבה
- **עורך טקסט עשיר** (Tiptap) עם סרגל: מודגש, נטוי, קו תחתון, קו חוצה, כותרות, רשימות, ציטוט, קישורים, יישור, בטל/חזור
- שדות To, CC, BCC (CC/BCC מתקפלים)
- HTML body ו-plain text נשלחים ל-API
- אזור גרירת קבצים (UI מוכן, backend בהמשך)

### השב / השב לכולם / העבר
- **השב** — ממלא To = השולח המקורי, נושא = `Re: {subject}`, גוף מקורי כציטוט
- **השב לכולם** — ממלא את כל הנמענים בהסרת עצמי
- **העבר** — נושא = `Fwd: {subject}`, כולל כותרת הודעה מועברת

### מחיקה / אשפה
- מחיקה מעבירה הודעות לאשפה דרך IMAP (COPY + DELETE + EXPUNGE)
- שחזור מעביר הודעות מאשפה חזרה ל-INBOX
- תיקיית אשפה מוצגת בסרגל הצד

### מעקב קריאה
- הודעות מסומנות כנקראו (`is_read`) בפתיחה דרך GET detail
- הודעות שלא נקראו מוצגות בטקסט מודגש ונקודה צבעונית

### רינדור אימייל HTML
- אימיילים נכנסים ב-HTML עוברים סניטציה עם DOMPurify ומוצגים ויזואלית
- fallback לטקסט פשוט עם `whitespace-pre-wrap`
- CSS מבודד (`prose`) לתצוגה בטוחה

## Phase 1

- דומיין **פלטפורמה** אחד מ-`MAIL_DOMAIN` (שורה ב-DB)
- יצירה/מחיקת תיבות בחשבון Platform Mail
- לקוח אימייל מקצועי 3-פאנלים עם כתיבה, השבה, העברה, אשפה
- כתיבה עם עורך טקסט עשיר Tiptap
- רינדור HTML עם סניטציה DOMPurify
- תמיכה ב-CC/BCC בכתיבה
- מעקב קריאה/לא נקרא עם אינדיקטורים ויזואליים
- רמזי DNS בכרטיס המטא-דאטה

## לא ב-Phase 1 (בהמשך)

- **תבניות שליחה HBS** ורכיבים/חתימות מותאמים
- העברת ניהול DNS של הדואר ל-**Account → Domains**
- העלאה/הורדת קבצים מצורפים (UI מוכן, backend בהמשך)
- spam, אוטומציה, דומיינים ל-tenant
- Access Key gateway
- webhook inbound
- מעבר OTP/invites ל-`noreply@`

## Config

ראה את עמוד ה-EN לפרטי env ודוגמאות API. פתחו בראוטר **25 / 465 / 587**; אל תחשפו את פורט הניהול הציבורי.

!!! note "Stalwart 0.16 — listeners"
    ב-Stalwart 0.16 הגדרות listeners נשמרות ב-data store ולא ב-`config.json`.
    IMAP (143) ו-submission (587) **לא** נוצרים בברירת מחדל — יש ליצור אותם
    דרך JMAP (`x:NetworkListener/set`) או WebUI ולהפעיל מחדש את ה-pod.
    ה-Helm chart חושף `hostPort: 143` ו-`hostPort: 993` כדי שה-API על ה-host יגיע ל-IMAP.
